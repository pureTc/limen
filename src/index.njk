---
layout: base.njk
title: limen | Selected Work
---

<main class="main-content">
  <div class="section-title">ENTRIES</div>
  
  <div class="project-list">
    {% for item in collections.posts %}
      <a href="{{ item.url }}" class="project-item-link">
        <div class="project-item">
          <div class="project-header">
            <div class="project-meta">
              <span class="project-id">{{ item.data.id }}</span> – <span class="project-name">{{ item.data.title }}</span>
              <div class="project-doc">{{ item.data.doc }}</div>
            </div>
            {% if item.data.hasIcon %}
              <span class="project-icon">⌘</span>
            {% endif %}
          </div>
          
          <p class="project-desc">{{ item.templateContent | striptags | truncate(220) }}</p>
        </div>
      </a>
    {% endfor %}
  </div>
</main>

<aside class="sidebar">
  <div class="sidebar-header">
    <a href="mailto:be@limen.me" class="email">be@limen.me</a>
    <span class="time" id="current-time">00:00:00</span>
  </div>

  <hr class="sidebar-divider">

<div class="biography">
  <p>花园里边明晃晃的，红的红，绿的绿，新鲜漂亮。</p>
  
  <p>花开了，就像睡醒了似的。鸟飞了，就像在天上逛似的。虫子叫了，就像虫子在说话似的。一切都活了，都有无限的本领，要做什么，就做什么。要怎么样，就怎么样，都是自由的。</p>
  
  <p>倭瓜愿意爬上房就爬上房，愿意爬上树就爬上树。黄瓜愿意开一个谎花，就开一个谎花，愿意结一个黄瓜，就结一个黄瓜。若都不愿意，就是一个黄瓜也不结，一朵花也不开，也没有人问它。玉米愿意长多高就长多高，他若愿意长到天上去，也没有人管他。蝴蝶飞得那么快，大红蝴蝶满天飞，白蝴蝶随风滚，要是追一只蝴蝶，那蝴蝶飞上了天，像同太阳在一起了似的，那便是追不上了。--- 呼兰河传</p>
</div>
</aside>

<script>
  function updateTime() {
    const timeElement = document.getElementById('current-time');
    const now = new Date();
    const hours = String(now.getHours()).padStart(2, '0');
    const minutes = String(now.getMinutes()).padStart(2, '0');
    const seconds = String(now.getSeconds()).padStart(2, '0');
    timeElement.textContent = `${hours}:${minutes}:${seconds}`;
  }
  updateTime();
  setInterval(updateTime, 1000);
</script>